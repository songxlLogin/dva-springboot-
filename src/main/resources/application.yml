
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/songxl?serverTimezone=UTC
    username: root
    password: root

mybatis:
  mapper-locations: classpath:UserMapper.xml
  type-aliases-package: com.songxl.springbootdva.model
